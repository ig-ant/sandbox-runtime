//! `probe_token` — dumps the current process token contents for
//! verification (used by Group A of the test matrix).
//!
//! Phase 2 will fill this in: query `TokenUser`, `TokenGroups`,
//! `TokenRestrictedSids`, `TokenIntegrityLevel`, `TokenPrivileges`
//! and print a human-readable summary.

fn main() {
    todo!("phase 2: dump token info")
}
